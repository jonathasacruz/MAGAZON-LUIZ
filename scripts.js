function editBtn(btn) {
  var tabela, elements, categoria, descricao;

  elements = btn.parentElement.parentElement.getElementsByTagName("td");

  categoria = elements[0].innerText;
  descricao = elements[1].innerText;

  

  
}
